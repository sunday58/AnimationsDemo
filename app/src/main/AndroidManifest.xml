<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.animationsdemo">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".KeyFrameActivity">
        </activity>
        <activity android:name=".SecondTransition">
        </activity>
        <activity android:name=".TransitionActivity">
        </activity>
        <activity android:name=".VectorActivity">
        </activity>
        <activity android:name=".WifiAnimation">
        </activity>
        <activity android:name=".DrawablesAnim">
        </activity>
        <activity
                android:name=".MainActivity"
                android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>